providers = ["python"]

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["cd apps/api && python -m venv .venv","cd apps/api && . .venv/bin/activate && python -m pip install -r requirements.txt"]

[start]
cmd = "cd apps/api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
