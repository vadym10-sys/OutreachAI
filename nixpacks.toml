providers = ["python"]

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["cd apps/api && pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "cd apps/api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
